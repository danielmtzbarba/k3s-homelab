apiVersion: v1
kind: Service
metadata:
  name: quant-engine-sync
  namespace: quant-engine-mt5
  labels:
    app.kubernetes.io/part-of: quant-engine
    app.kubernetes.io/component: sync-bridge
spec:
  ports:
    - name: sync-api
      port: 8080
      targetPort: 8080

apiVersion: v1
kind: Service
metadata:
  name: danielmtz-website
  namespace: danielmtz-website
  labels:
    app.kubernetes.io/name: danielmtz-website
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: danielmtz-website
  ports:
    - name: http
      port: 80
      targetPort: http

apiVersion: v1
kind: Service
metadata:
  name: quant-engine-prod-messaging-service
  namespace: quant-engine-mt5
  labels:
    app.kubernetes.io/name: quant-engine-prod-messaging
    app.kubernetes.io/part-of: quant-engine
    environment: prod
spec:
  selector:
    app.kubernetes.io/name: quant-engine-prod-messaging
  ports:
    - name: http
      port: 8003
      targetPort: http

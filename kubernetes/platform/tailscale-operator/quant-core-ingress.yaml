apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: quant-core-private
  namespace: quant-engine-mt5
  annotations:
    tailscale.com/tags: "tag:k8s"
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - quant-core
  rules:
    - host: quant-core
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: quant-engine-dev-core-service
                port:
                  number: 8001
